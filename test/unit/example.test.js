/**
 * This is an example unit test. Unit tests should target a specific
 * "unit" of code, such as an exported namespace from a module, or a
 * single function execution.
 */

describe('Example unit test', function() {
  it('works', function() {
    expect(2 + 2).toEqual(4)
  })
})
